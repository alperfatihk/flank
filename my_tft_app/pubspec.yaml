name: mobalytics_clone_final
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Flutter SDK versiyonunuza göre değişebilir

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6 # Bu satır olmalı
  provider: ^6.0.5       # Bu satır olmalı (veya en güncel sürümü)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/ # Bu klasöre örnek resimlerinizi koyacaksınız